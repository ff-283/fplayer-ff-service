# 打包发布与使用教程（Windows）

本文档面向发布人员与最终使用者，说明如何生成可分发包，以及用户拿到包后如何直接运行。

## 1. 发布目标

发布结果以 `release/portable` 为准，要求：

- 用户只需双击 `FPlayerFFService.exe`
- 运行依赖从 exe 同级目录查找
- 不需要用户手动执行 `start-service.bat` 或 `scripts/start-all.ps1`

## 2. 发布前准备

在 `fplayer-ff-service` 项目根目录确认：

- 已安装 Go（用于编译 `gateway.exe`）
- 已安装 Node.js/npm（用于打包 Electron）
- `3rd/zlm/windows/MediaServer.exe` 存在
- `3rd/zlm/windows/config.ini` 存在

## 3. 一键打包发布

在项目根目录执行：

```powershell
.\scripts\build-release.ps1
```

脚本会自动完成：

1. 编译 `gateway/bin/gateway.exe`
2. 打包 Electron（生成 `ui/dist/win-unpacked` 与安装包 exe）
3. 组装 `release/portable`（推荐分发目录）
4. 校验关键依赖是否齐全，缺失则直接失败

### 3.1 清理后重打包（可选）

如果你希望先清理构建产物再发布，可先执行：

```powershell
.\scripts\clean.ps1
```

然后再执行：

```powershell
.\scripts\build-release.ps1
```

`build-release.ps1` 会重新生成发布需要的全部可再生产物（前提是 `3rd/` 等核心依赖仍在）。

## 4. 产物说明（release）

打包成功后，会看到：

- `release/portable/`（推荐分发）
- `release/win-unpacked/`（原始解包产物）
- `release/FPlayerFFService 0.1.0.exe`（安装包/便携单文件产物）
- `release/README-发布说明.txt`

推荐只分发 `release/portable` 整个目录。

## 5. 如何分发给用户

推荐方式：

1. 将 `release/portable` 整个目录打包（zip）
2. 发给用户并解压到任意目录
3. 用户双击 `portable/FPlayerFFService.exe` 启动

注意：不要只发单个 exe，必须连同同级依赖目录一起发。

## 6. 用户侧使用教程

### 6.1 启动服务

双击：

- `FPlayerFFService.exe`

程序会自动在后台拉起 service 内核（ZLM + gateway）。

### 6.2 等待“服务已就绪”

UI 会显示启动状态：

- `正在初始化服务...` / `正在启动 ZLM 与 gateway...`：启动中
- `服务已就绪（gateway: ...）`：可开始创建流
- `启动失败：...`：需查看日志排障

### 6.3 在 desktop 里填写服务地址

以运行后生成的文件为准：

- `run/runtime.json` 的 `gateway.url`

把这个地址填写到 desktop 的“服务地址”字段。

## 7. 最小自检清单（发布前）

在 `release/portable` 下确认至少存在：

- `FPlayerFFService.exe`
- `ffmpeg.dll`
- `3rd/zlm/windows/MediaServer.exe`
- `3rd/zlm/windows/config.ini`
- `gateway/bin/gateway.exe`
- `resources/app.asar`

缺任一项都不应分发。

## 8. 常见问题

### 8.1 点击“创建并开始”报 `Failed to fetch`

优先检查：

- UI 状态是否已到“服务已就绪”
- `run/runtime.json` 是否已生成
- `logs/gateway.err.log` 是否有启动错误
- desktop 填写的是否是 `runtime.json` 中的 `gateway.url`

### 8.2 提示缺少 `ffmpeg.dll`

说明分发包不完整。请重新执行 `build-release.ps1`，并分发完整的 `release/portable` 目录。

### 8.3 端口冲突

当前版本会自动选择可用端口，实际端口以 `run/runtime.json` 为准。
